# URL_shortener
A small project to shorten URLs but the main aspect about it is that I am making use of K8s, docker and jenkins. So I am able to have multiple microservices for efficiency instead of a single one.
